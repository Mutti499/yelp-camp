<nav class="navbar navbar-dark sticky-top navbar-expand-lg bg-dark"  data-bs-theme="dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/campgrounds">YelpCamp</a>
      <% if (user) { %>
        <div class="dropdown d-lg-none me-auto">
          <button class="btn btn-sm btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
            <label class="navbar-text d-lg-none" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Welcome <%= user.username %></label>
          </button>

        <ul class="dropdown-menu">
          <li><a class="dropdown-item" href="/users/<%=user.id %>">Show Profile</a></li>
          <li><a class="dropdown-item" href="/change-password/<%=user.id %>">Settings</a></li>
          <li><a class='dropdown-item' href="/logout">Logout</a></li>
        </ul>
      </div>
      <% }%>  
      <button class="navbar-toggler bg-muted" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon "></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav">
          <a class="nav-link" href="/">Home</a>
          <a class="nav-link" href="/campgrounds">Campgrounds</a>
          <a class="nav-link" href="/campgrounds/new">Add New</a>
          <% if (user) { %>
            <a class="nav-link  d-lg-none" href="/change-password/<%=user.id %>">Settings</a>
          <% }%>  
        </div>
        <div class="navbar-nav ms-auto">
          <% if (!user) { %>
            <a class='nav-link' href="/login">Login</a>
          <% } else {%>
            <div class="dropdown d-none d-lg-inline">
                <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                  <label class="navbar-text d-lg-inline d-none" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Welcome <%= user.username %></label>
                </button>

              <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="/users/<%=user.id %>">Show Profile</a></li>
                <li><a class="dropdown-item" href="/change-password/<%=user.id %>">Settings</a></li>
                <li><a class='dropdown-item' href="/logout">Logout</a></li>
              </ul>
            </div>
          <% } %>         
        </div>
      </div>
    </div>
  </nav>